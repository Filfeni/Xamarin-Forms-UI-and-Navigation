<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_UI_Navigation.Register"
             Title="Register">
    
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Image Margin="50,0" Scale="1" HeightRequest="200" WidthRequest="200" Source="emblemaintec.png" />
            <Entry Text="{Binding User.Name}" Placeholder="Name"></Entry>
            <Entry Text="{Binding User.Email}" Placeholder="Email"></Entry>
            <Entry Text="{Binding User.Password}" Placeholder="Password" IsPassword="True"></Entry>
            <Entry Text="{Binding User.ConfirmPassword}" Placeholder="Confirm Password" IsPassword="True"></Entry>
            <Button Command="{Binding RegisterCommand}"  BackgroundColor="#d90812" Text="Register" TextColor="#ffffff"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>